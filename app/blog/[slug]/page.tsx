import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {BasicContentPage} from "../../../components/basic-content-page";import {blogArticles,getArticle,getCategory} from "../../../lib/blog";
export const dynamicParams=false;export function generateStaticParams(){return blogArticles.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const a=getArticle((await params).slug);if(!a)return{robots:{index:false}};return{title:`${a.title} | Viagem Perfeita`,description:a.excerpt,keywords:a.keywords,alternates:{canonical:`/blog/${a.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const a=getArticle((await params).slug);if(!a)notFound();return <BasicContentPage eyebrow={getCategory(a.category)?.name||"Conteúdo"} title={a.title} description={a.excerpt}><p className="article-meta">Leitura estimada: {a.readingTime}</p>{a.sections.map(section=><section className="article-section" key={section.title}><h2>{section.title}</h2><p>{section.content}</p></section>)}<p className="content-notice">Conteúdo informativo. Regras migratórias, sanitárias e operacionais podem mudar; consulte sempre fontes oficiais.</p><Link className="text-link" href={`/blog/categoria/${a.category}`}>Ver mais conteúdos desta categoria</Link></BasicContentPage>}
