import type { ReactNode } from "react";
export function PageHero({ eyebrow, title, description, image, children }: { eyebrow:string; title:string; description:string; image?:string; children?:ReactNode }) { return <section className="inner-hero" style={image?{backgroundImage:`linear-gradient(90deg,rgba(5,45,72,.94),rgba(5,45,72,.42)),url(${image})`}:undefined}><div className="shell"><p className="eyebrow light">{eyebrow}</p><h1>{title}</h1><p>{description}</p>{children}</div></section> }
