"use client";
import { useEffect,useState } from "react";
import { AlertTriangle,CalendarCheck,CheckCircle2,Clock3,Users } from "lucide-react";
import { getSupabaseBrowserClient } from "../lib/supabase-client";
type Metrics={today:number;month:number;unassigned:number;overdue:number;confirmed:number};
export function AdminDashboard(){const[m,setM]=useState<Metrics>({today:0,month:0,unassigned:0,overdue:0,confirmed:0});const[error,setError]=useState("");useEffect(()=>{(async()=>{const s=getSupabaseBrowserClient();if(!s)return;const now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()).toISOString(),month=new Date(now.getFullYear(),now.getMonth(),1).toISOString();const[a,b,c,d,e]=await Promise.all([s.from("leads").select("id",{count:"exact",head:true}).gte("created_at",today),s.from("leads").select("id",{count:"exact",head:true}).gte("created_at",month),s.from("leads").select("id",{count:"exact",head:true}).is("assigned_to",null),s.from("tasks").select("id",{count:"exact",head:true}).lt("due_at",now.toISOString()).neq("status","concluida"),s.from("reservations").select("id",{count:"exact",head:true}).eq("status","confirmada")]);if(a.error){setError("Não foi possível carregar os indicadores.");return}setM({today:a.count??0,month:b.count??0,unassigned:c.count??0,overdue:d.count??0,confirmed:e.count??0})})()},[]);const items=[[Users,"Novos leads hoje",m.today],[CalendarCheck,"Leads no mês",m.month],[Clock3,"Sem atendimento",m.unassigned],[AlertTriangle,"Tarefas vencidas",m.overdue],[CheckCircle2,"Reservas confirmadas",m.confirmed]] as const;return <>{error?<div className="crm-alert">{error}</div>:null}<div className="crm-metrics">{items.map(([Icon,label,value])=><div key={label}><Icon/><span>{label}</span><b>{value}</b></div>)}</div><section className="crm-panel"><h2>Visão comercial</h2><p>Indicadores são calculados exclusivamente com registros reais do Supabase. Filtros e gráficos serão preenchidos conforme a operação gerar dados.</p></section></>}
