"use client";
import { useEffect,useState } from "react";
import { AlertTriangle,CalendarCheck,CheckCircle2,Clock3,MailWarning,Users } from "lucide-react";
import { getSupabaseBrowserClient } from "../lib/supabase-client";
type Metrics={today:number;month:number;unassigned:number;overdue:number;confirmed:number;emailFailures:number;emailPending:number};
const empty:Metrics={today:0,month:0,unassigned:0,overdue:0,confirmed:0,emailFailures:0,emailPending:0};
export function AdminDashboard(){const[m,setM]=useState<Metrics>(empty);const[error,setError]=useState("");useEffect(()=>{(async()=>{const s=getSupabaseBrowserClient();if(!s)return;const now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()).toISOString(),month=new Date(now.getFullYear(),now.getMonth(),1).toISOString();const[a,b,c,d,e,f,g]=await Promise.all([s.from("leads").select("id",{count:"exact",head:true}).gte("created_at",today),s.from("leads").select("id",{count:"exact",head:true}).gte("created_at",month),s.from("leads").select("id",{count:"exact",head:true}).is("assigned_to",null),s.from("tasks").select("id",{count:"exact",head:true}).lt("due_at",now.toISOString()).neq("status","concluida"),s.from("reservations").select("id",{count:"exact",head:true}).eq("status","confirmada"),s.from("email_notifications").select("id",{count:"exact",head:true}).eq("status","falhou"),s.from("email_notifications").select("id",{count:"exact",head:true}).in("status",["pendente","processando"])]);if(a.error){setError("Não foi possível carregar os indicadores.");return}setM({today:a.count??0,month:b.count??0,unassigned:c.count??0,overdue:d.count??0,confirmed:e.count??0,emailFailures:f.count??0,emailPending:g.count??0})})()},[]);const items=[[Users,"Novos leads hoje",m.today],[CalendarCheck,"Leads no mês",m.month],[Clock3,"Sem atendimento",m.unassigned],[AlertTriangle,"Tarefas vencidas",m.overdue],[CheckCircle2,"Reservas confirmadas",m.confirmed],[MailWarning,"Falhas de e-mail",m.emailFailures],[Clock3,"E-mails pendentes",m.emailPending]] as const;return <>{error?<div className="crm-alert">{error}</div>:null}<div className="crm-metrics">{items.map(([Icon,label,value])=><div key={label}><Icon/><span>{label}</span><b>{value}</b></div>)}</div><section className="crm-panel"><h2>Visão comercial</h2><p>Indicadores calculados exclusivamente com registros reais do Supabase, incluindo a saúde da segunda camada de notificações.</p></section></>}
