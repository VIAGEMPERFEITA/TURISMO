"use client";
import {useCallback,useEffect,useMemo,useState} from "react";
import {Bot,CheckCheck,Circle,Clock3,Filter,MessageCircle,RefreshCw,Search,Send,UserRoundCheck,Users} from "lucide-react";
import {getSupabaseBrowserClient} from "../lib/supabase-client";
type Conversation={id:string;channel:"whatsapp"|"instagram"|"facebook";status:string;control_mode:string;priority:string;assigned_to:string|null;queue_id:string|null;last_message_at:string|null;last_customer_message_at:string|null;first_response_at:string|null;unread_count:number;ai_handoff_summary:string|null;contact_wa_id:string|null;external_thread_id:string|null;lead:{name:string;phone:string;email:string|null;city:string|null;state:string|null;source:string|null;status:string|null;qualification_data:Record<string,unknown>|null}|null;assignee:{full_name:string}|null};
type Message={id:string;direction:string;author_type:string;body:string|null;sent_at:string;delivery_status:string|null;error_message:string|null};
type Profile={id:string;full_name:string;role:string};
const labels:Record<string,string>={ia_ativa:"IA ativa",aguardando_equipe:"Aguardando equipe",humano_ativo:"Humano ativo",aguardando_cliente:"Aguardando cliente",retorno_ia:"Retorno à IA",encerrada:"Encerrada",aberta:"Aberta"};
const channelLabels:Record<Conversation["channel"],string>={whatsapp:"WhatsApp",instagram:"Instagram",facebook:"Messenger"};
const when=(value:string|null)=>value?new Date(value).toLocaleString("pt-BR",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"}):"—";
export function WhatsAppInbox(){
 const[conversations,setConversations]=useState<Conversation[]>([]),[messages,setMessages]=useState<Message[]>([]),[profiles,setProfiles]=useState<Profile[]>([]);const[selectedId,setSelectedId]=useState<string|null>(null),[query,setQuery]=useState(""),[channelFilter,setChannelFilter]=useState("todos"),[statusFilter,setStatusFilter]=useState("todos"),[draft,setDraft]=useState(""),[notice,setNotice]=useState(""),[loading,setLoading]=useState(true),[sending,setSending]=useState(false);const selected=conversations.find(item=>item.id===selectedId)||null;
 const loadConversations=useCallback(async()=>{const client=getSupabaseBrowserClient();if(!client)return;const{data,error}=await client.from("conversations").select("id,channel,status,control_mode,priority,assigned_to,queue_id,last_message_at,last_customer_message_at,first_response_at,unread_count,ai_handoff_summary,contact_wa_id,external_thread_id,lead:leads(name,phone,email,city,state,source,status,qualification_data),assignee:profiles!conversations_assigned_to_fkey(full_name)").in("channel",["whatsapp","instagram","facebook"]).neq("status","encerrada").order("last_message_at",{ascending:false,nullsFirst:false}).limit(200);setLoading(false);if(error){setNotice("Não foi possível carregar as conversas com a permissão atual.");return}const rows=(data||[])as unknown as Conversation[];setConversations(rows);setSelectedId(current=>current&&rows.some(row=>row.id===current)?current:rows[0]?.id||null)},[]);
 const loadMessages=useCallback(async(id:string)=>{const client=getSupabaseBrowserClient();if(!client)return;const{data,error}=await client.from("messages").select("id,direction,author_type,body,sent_at,delivery_status,error_message").eq("conversation_id",id).order("sent_at",{ascending:true}).limit(500);if(error)setNotice("Não foi possível carregar o histórico desta conversa.");else setMessages((data||[])as Message[])},[]);
 useEffect(()=>{const client=getSupabaseBrowserClient();if(!client)return;loadConversations();client.from("profiles").select("id,full_name,role").eq("active",true).neq("role","visualizador").order("full_name").then(({data})=>setProfiles((data||[])as Profile[]));client.rpc("set_agent_presence",{target_status:"online",accept_automatic:true,target_capacity:8});return()=>{client.rpc("set_agent_presence",{target_status:"offline",accept_automatic:false,target_capacity:8})}},[loadConversations]);
 useEffect(()=>{if(selectedId){loadMessages(selectedId);const client=getSupabaseBrowserClient();client?.rpc("mark_conversation_read",{target_conversation_id:selectedId}).then(()=>setConversations(rows=>rows.map(row=>row.id===selectedId?{...row,unread_count:0}:row)))}else setMessages([])},[selectedId,loadMessages]);
 useEffect(()=>{const client=getSupabaseBrowserClient();if(!client)return;const channel=client.channel("vp-whatsapp-inbox").on("postgres_changes",{event:"*",schema:"public",table:"conversations"},()=>loadConversations()).on("postgres_changes",{event:"INSERT",schema:"public",table:"messages"},payload=>{const row=payload.new as{conversation_id?:string};if(row.conversation_id===selectedId&&selectedId)loadMessages(selectedId);loadConversations()}).subscribe();return()=>{client.removeChannel(channel)}},[loadConversations,loadMessages,selectedId]);
 const filtered=useMemo(()=>conversations.filter(item=>`${item.lead?.name||""} ${item.lead?.phone||""} ${channelLabels[item.channel]}`.toLowerCase().includes(query.toLowerCase())&&(channelFilter==="todos"||item.channel===channelFilter)&&(statusFilter==="todos"||statusFilter==="nao_lidas"&&item.unread_count>0||statusFilter==="humano"&&item.control_mode==="humano"||statusFilter==="ia"&&item.control_mode==="ia"||item.status===statusFilter)),[conversations,query,channelFilter,statusFilter]);
 const stats=useMemo(()=>({total:conversations.length,unread:conversations.reduce((sum,item)=>sum+item.unread_count,0),human:conversations.filter(item=>item.control_mode==="humano"||item.status==="aguardando_equipe").length,urgent:conversations.filter(item=>["urgente","alta"].includes(item.priority)).length}),[conversations]);
 const lastInbound=[...messages].reverse().find(message=>message.direction==="entrada");
 const suggestedReplies=["Olá! Sou da equipe Viagem Perfeita. Vou continuar seu atendimento por aqui.","Obrigado pelas informações. Vou conferir os dados oficiais e retorno com a orientação correta.","Para ajudar melhor, qual destino e período você tem em mente?","Vou transferir esta solicitação para um consultor responsável continuar com você."];
 async function rpc(name:string,args:Record<string,unknown>){const client=getSupabaseBrowserClient();if(!client)return;setNotice("");const{error}=await client.rpc(name,args);setNotice(error?`A ação não foi concluída: ${error.message}`:"Ação concluída.");await loadConversations();if(selectedId)await loadMessages(selectedId)}
 async function send(){const client=getSupabaseBrowserClient(),text=draft.trim();if(!client||!selected||!text||sending)return;if(selected.channel!=="whatsapp"){setNotice("O envio humano deste canal será liberado após o teste externo controlado.");return}setSending(true);setNotice("");const{data,error}=await client.functions.invoke("whatsapp-agent-send",{body:{conversationId:selected.id,message:text,idempotencyKey:crypto.randomUUID()}});setSending(false);if(error||data?.status!=="enviado"){setNotice(data?.error?`Envio não concluído: ${data.error}`:"Não foi possível enviar a mensagem.");return}setDraft("");setNotice("Mensagem enviada.");await loadMessages(selected.id)}
 return <section className="wa-inbox" aria-label="Caixa de entrada omnichannel"><aside className="wa-list"><header><div><small>ATENDIMENTO OMNICHANNEL</small><h2>Conversas</h2></div><button onClick={loadConversations} aria-label="Atualizar"><RefreshCw/></button></header><div className="campaign-rates"><span>Total <b>{stats.total}</b></span><span>Não lidas <b>{stats.unread}</b></span><span>Humano <b>{stats.human}</b></span><span>Prioridade <b>{stats.urgent}</b></span></div><label className="wa-search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nome, telefone ou canal"/></label><div className="wa-actions"><Filter/><select aria-label="Filtrar canal" value={channelFilter} onChange={e=>setChannelFilter(e.target.value)}><option value="todos">Todos os canais</option><option value="whatsapp">WhatsApp</option><option value="instagram">Instagram</option><option value="facebook">Messenger</option></select><select aria-label="Filtrar situação" value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="todos">Todas as situações</option><option value="nao_lidas">Não lidas</option><option value="ia">Com IA</option><option value="humano">Com atendente</option><option value="aguardando_equipe">Aguardando equipe</option></select></div><div className="wa-conversation-list">{loading?<p>Carregando…</p>:filtered.length?filtered.map(item=><button className={item.id===selectedId?"active":""} onClick={()=>setSelectedId(item.id)} key={item.id}><span className="wa-avatar">{(item.lead?.name||channelLabels[item.channel]).slice(0,1).toUpperCase()}</span><span><strong>{item.lead?.name||item.contact_wa_id||"Contato"}{item.unread_count>0?` · ${item.unread_count} nova(s)`:""}</strong><small>{channelLabels[item.channel]} · {labels[item.status]||item.status} · {when(item.last_message_at)}</small></span>{["urgente","alta"].includes(item.priority)?<i/>:null}</button>):<p>Nenhuma conversa encontrada.</p>}</div></aside><main className="wa-thread">{selected?<><header><div><strong>{selected.lead?.name||`Contato ${channelLabels[selected.channel]}`}</strong><small>{channelLabels[selected.channel]} · {selected.lead?.phone||selected.contact_wa_id||selected.external_thread_id||"Identidade protegida"} · {selected.assignee?.full_name||"Sem responsável"}</small></div><span className={`wa-mode ${selected.control_mode}`}><Circle/>{selected.control_mode==="ia"?"IA atendendo":selected.control_mode==="humano"?"Atendimento humano":"Em espera"}</span></header><div className="wa-actions"><button onClick={()=>rpc("claim_conversation",{target_conversation_id:selected.id})}><UserRoundCheck/>Assumir</button><label><Users/><select aria-label="Transferir conversa" value="" onChange={e=>e.target.value&&rpc("transfer_conversation",{target_conversation_id:selected.id,target_profile_id:e.target.value,target_queue_id:null,transfer_reason:"Transferência pela caixa de entrada"})}><option value="">Transferir…</option>{profiles.map(profile=><option value={profile.id} key={profile.id}>{profile.full_name}</option>)}</select></label><button onClick={()=>rpc("return_conversation_to_ai",{target_conversation_id:selected.id,context_summary:"Atendimento humano concluído pela caixa de entrada.",resume_at:new Date().toISOString()})}><Bot/>Devolver à IA</button></div><div className="wa-messages">{messages.map(message=><article className={`${message.direction} ${message.author_type}`} key={message.id}><small>{message.author_type==="cliente"?selected.lead?.name||"Cliente":message.author_type==="ia"?"Assistente IA":message.author_type==="humano"?"Consultor":"Sistema"}</small><p>{message.body||`[${message.author_type}]`}</p>{message.delivery_status==="falhou"&&message.error_message?<small role="alert">Falha de entrega: {message.error_message}</small>:null}<time>{when(message.sent_at)}{message.direction==="saida"?<CheckCheck aria-label={message.delivery_status||"pendente"}/>:null}</time></article>)}</div>{selected.control_mode==="humano"&&selected.channel==="whatsapp"?<div className="wa-actions">{suggestedReplies.map(text=><button key={text} onClick={()=>setDraft(text)}>{text.slice(0,42)}…</button>)}</div>:null}{notice?<div className="wa-notice" role="status">{notice}</div>:null}<footer><textarea value={draft} onChange={e=>setDraft(e.target.value)} placeholder={selected.control_mode==="humano"?(selected.channel==="whatsapp"?"Digite sua mensagem…":"Envio externo será liberado após o teste controlado"):"Assuma a conversa antes de responder"} disabled={selected.control_mode!=="humano"||selected.channel!=="whatsapp"} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();send()}}}/><button onClick={send} disabled={selected.control_mode!=="humano"||selected.channel!=="whatsapp"||!draft.trim()||sending} aria-label="Enviar mensagem"><Send/></button></footer></>:<div className="wa-thread-empty"><MessageCircle/><h2>Selecione uma conversa</h2><p>As mensagens recebidas aparecem aqui em tempo real.</p></div>}</main><aside className="wa-details">{selected?<><h3>Cliente 360º</h3><dl><dt>Canal</dt><dd>{channelLabels[selected.channel]}</dd><dt>Status</dt><dd>{labels[selected.status]||selected.status}</dd><dt>Prioridade</dt><dd>{selected.priority}</dd><dt>Responsável</dt><dd>{selected.assignee?.full_name||"Sem responsável"}</dd><dt>Telefone</dt><dd>{selected.lead?.phone||selected.contact_wa_id||"—"}</dd><dt>E-mail</dt><dd>{selected.lead?.email||"—"}</dd><dt>Cidade</dt><dd>{[selected.lead?.city,selected.lead?.state].filter(Boolean).join("/")||"—"}</dd><dt>Origem</dt><dd>{selected.lead?.source||channelLabels[selected.channel]}</dd><dt>Última mensagem</dt><dd><Clock3/>{when(selected.last_message_at)}</dd><dt>Primeira resposta</dt><dd>{when(selected.first_response_at)}</dd></dl><div className="wa-ai-note"><Bot/><div><strong>Leitura rápida da IA</strong><p>{selected.ai_handoff_summary||lastInbound?.body||"Ainda não há conteúdo suficiente para resumir esta conversa."}</p></div></div><div className="wa-ai-note"><Bot/><div><strong>Controle IA/humano</strong><p>Ao assumir, a IA pausa imediatamente. Ao devolver, ela recebe o histórico e pode continuar.</p></div></div></>:null}</aside></section>
}
