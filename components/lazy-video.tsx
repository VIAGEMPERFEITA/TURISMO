"use client";
import { Play } from "lucide-react";
import { useState } from "react";
export function LazyVideo({src,poster,label,orientation="horizontal"}:{src:string;poster:string;label:string;orientation?:"vertical"|"horizontal"}){const[active,setActive]=useState(false);return <div className={`video-frame video-frame-${orientation}`}>{active?<video controls autoPlay playsInline preload="metadata" poster={poster} aria-label={label}><source src={src} type="video/mp4"/><p>Não foi possível reproduzir. <a href={src}>Abra o vídeo diretamente</a>.</p></video>:<button className="video-poster" type="button" onClick={()=>setActive(true)} style={{backgroundImage:`linear-gradient(rgba(4,25,20,.08),rgba(4,25,20,.18)),url(${poster})`}}><span><Play fill="currentColor"/>Assistir vídeo completo</span></button>}</div>}
